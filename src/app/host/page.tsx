export default function HostPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="font-mono text-fg-muted text-sm">/host — pre-build</p>
    </main>
  );
}
