// Placeholder. Tables get added during the build (rooms, players, scores, events).
// Per mission §4: state is ephemeral per session — schema lives here, no migrations until first feature.
export {};
